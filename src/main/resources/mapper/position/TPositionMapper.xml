<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.just.ekao.sys.pos.dao.TPositionMapper">
    <select id="findBYDepId" resultType="com.just.ekao.sys.pos.pojo.TPosition">
    select * from t_position t where t.pos_dep_id=#{depId}
  </select>
</mapper>